{
  "id": "VersionStampBuilderSchema",
  "title": "Version Stamp builder",
  "description": "Version Stamp builder options",
  "properties": {
    "baseFilename": {
      "type": "string",
      "description": "Filename for the env file in ts format which gets created",
      "default": "version-info"
    }
  }
}
