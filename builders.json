{
  "$schema": "@angular-devkit/architect/src/builders-schema.json",
  "builders": {
    "builder-name": {
      "class": "./src/version-stamp.builder.js",
      "schema": "version-stamp.schema.json",
      "description": "Builder which stamps the current version from package.json & `git describe` output into an env ts file"
    }
  }
}
